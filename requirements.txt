fastapi
uvicorn
pymongo
motor
python-dotenv
PyJWT
cryptography
pydantic
PyJWT[crypto]
pydantic[email]
resend
tzdata
pytest
pytest-asyncio
playwright

# errAgent SDK — replaces the vendored erragent_handler.py + hand-rolled ingest/client-error
# HTTP clients. Installed from GitHub, not a local path — a relative "-e ../errAgent/sdk" only
# resolves on a machine where errAgent happens to be checked out as a sibling directory; it
# would fail during a Render (or any CI) build, which checks out this repo in isolation. Once
# erragent-sdk is published to PyPI, switch this to a plain "erragent-sdk" version pin instead.
git+https://github.com/SummonShenron/errAgent.git@sdk-v0.1.0#subdirectory=sdk